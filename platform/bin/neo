#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
BACKEND="${ROOT}/backend"
HOST="${NEO_HOST:-127.0.0.1}"
PORT="${NEO_PORT:-8787}"

cd "${BACKEND}"
exec uv run uvicorn neo.app:app --host "${HOST}" --port "${PORT}" --reload
