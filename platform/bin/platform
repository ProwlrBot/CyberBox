#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
BACKEND="${ROOT}/backend"
HOST="${CYBERBOX_HOST:-${NEO_HOST:-127.0.0.1}}"
PORT="${CYBERBOX_PORT:-${NEO_PORT:-8787}}"

cd "${BACKEND}"
exec uv run uvicorn cyberbox.app:app --host "${HOST}" --port "${PORT}" --reload
